import { Router } from "express";
import BookController from "../controller/book.controller";
export declare function createBookRoutes(bookController: BookController): Router;
//# sourceMappingURL=book.routes.d.ts.map